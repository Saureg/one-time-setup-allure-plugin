group 'ru.pravo.qa'
version '1.0-SNAPSHOT'

apply plugin: 'java-library-distribution'

sourceCompatibility = 1.8

repositories {
    jcenter()
}

jar {
    archiveName = 'one-time-setup-allure-plugin.jar'
}

dependencies {
    compileOnly('io.qameta.allure:allure-plugin-api:2.5.0'
    )
}
